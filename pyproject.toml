[project]
name = "simplemanus"
version = "0.1.0"
description = "A universal agent framework"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "simplellmfunc==0.3.1",
    "fastapi",
    "uvicorn",
    "pydantic",
    "websocket",
    "websockets",
    "pydantic-core",
    "redis",
    "mongoengine>=0.29.1",
]
